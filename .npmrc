# Keep optional peer integrations opt-in. In particular, @vitejs/plugin-react
# exposes React Compiler and Rolldown/Babel integrations that this project does
# not use.
engine-strict=true
legacy-peer-deps=true
